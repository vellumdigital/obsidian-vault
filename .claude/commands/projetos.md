---
description: Lista projetos do vault com status visual (emojis, barra de progresso, proxima acao)
allowed-tools: Bash(proj-list:*)
---

Roda `proj-list` e retorna o output exatamente como veio (formato pronto para Telegram).
